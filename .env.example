# n8n webhook URL — copy from your n8n workflow after activating it
# Use the TEST URL (webhook-test) for local development
# Use the PRODUCTION URL (webhook) for Vercel deployment
N8N_WEBHOOK_URL=https://verkkosite.app.n8n.cloud/webhook/nail-generate
